# Windows WSL Diagnostics

Improved stderr and path diagnostics for Windows WSL builds so failures retain
their actionable command context.

Verification: Windows WSL behavior tests and shell checks.
