# CI Stability

Stabilized the CI checks around persisted gateway history and repeatable test
startup, while preserving the existing runtime contracts.

Verification: full host test suite and CI script checks.
