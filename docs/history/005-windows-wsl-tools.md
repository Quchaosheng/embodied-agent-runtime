# Windows WSL Tools

Added the Windows and WSL development entry points, with path-safe handling,
environment checks, and repeatable local smoke commands.

Verification: PowerShell syntax checks and Windows WSL behavior tests.
